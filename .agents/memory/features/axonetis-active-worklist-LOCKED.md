---
name: AXONETIS Active Worklist LOCKED Jul 6 2026
description: Founder-locked bari-bari sequence of remaining AXONETIS tasks. One at a time. No mixing. No jumping. Reference this before starting any work.
type: feature
---

# AXONETIS Active Worklist (LOCKED — bari-bari, ek waqt ek)

**Rule:** Sirf ek item at a time. Founder "agla" bolay tab next. Mixing = reject.

## In progress ⚠️
0. **Rust server: Smart Hybrid + Sherlock manual-mode** — apply karna hai
   - OR1-first → 80% daily quota trip → OR2 warm backup
   - Public users OR1 se BLOCKED (server-side)
   - Sherlock auto-audit BAND (only fires on `/scan` or user command)
   - Background/cron jobs BAND
   - Deliverable: single copy-paste `src/main.rs` overwrite block + rebuild + PM2 restart + 2 smoke tests

## Pending ❌ (locked order)
1. **Panels real `/rpc/*` wiring** — currently stubs, replace with real endpoints
2. **3.9.1 Chatbox cinematic polish** — shimmer on new msg, inline diff-preview card, tool-call mini-card (progress+cost), voice waveform, @mention picker, /slash palette, stop button, visual-edit hook, cost meter in header
3. **3.9.2 Sidebar** — Files / Cloud(DB/Storage/Secrets) / Tools / Costs / Security / Logs / Analytics real endpoints
4. **3.9.3 Domains** — DNS table, Caddy activate, primary toggle
5. **3.9.4 Versions** — Monaco diff, restore, Sherlock badge
6. **3.10 Tool Registry** — 12 tools + Zod + permission matrix + activity stream + diff modal + cost meter + sub-agent timeline + mem editor + self-verify UI + LSP inline
7. **3.10.9 Agents Runtime** — Hetzner `agents.routes.ts v2` (streamText + tool() + stopWhen(50) + SSE)
8. **3.10.10 Supabase 3 migration** — `tool_calls`, `agent_subagents`, `mem_entries` (GRANT + RLS + policy same file)
9. **Phase 9** — Trojan Horse CRM (inside Founder OS)
10. **Phase 10** — Advantage Layer (16 features)
11. **Phase 11** — Outreach Engine
12. **Phase 12** — Final Lock (Settings + Onboarding + Help)

**Then and only then:** ANEXVOT AI Pay (deferred, very end).

## Enforcement
- Skip request → reject, reference this file.
- Parallel work request → reject, ek waqt ek.
- Every completed item → strike + append audit line with date.
