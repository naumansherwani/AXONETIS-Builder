# Add INSIDE the existing founderbuilder.axonetis.com @founder_only handle block.
# Caddy auto-handles WebSocket Upgrade — no extra directives needed.
# Founder-only SSH bridge: /ssh → 127.0.0.1:8092

handle_path /ssh* {
    reverse_proxy 127.0.0.1:8092
}
